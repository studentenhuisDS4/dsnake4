import { KEY } from '../imports';

export class KeyBindings {
    /* Optimalization to handle keys easily */
    A!: KEY;
    W!: KEY;
    S!: KEY;
    D!: KEY;
    LEFT!: KEY;
    DOWN!: KEY;
    RIGHT!: KEY;
    UP!: KEY;
}